# Waitlist submission endpoint (POST). Optional — defaults to the live backend
# at https://dev-api.currenta.tech/api/waitlist (backend/api/waitlist.py).
#
# Set this to point local dev or a staging build at a different backend.
# Copy to `.env` (gitignored) for local dev.
VITE_WAITLIST_ENDPOINT=
